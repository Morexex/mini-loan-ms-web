# Must match the API APP_URL host+port from mini-loan-ms-api/.env
# Default if API is on Laravel's usual port:
VITE_API_URL=http://127.0.0.1:8000
#
# If API is started with --port=8001, use:
# VITE_API_URL=http://127.0.0.1:8001
