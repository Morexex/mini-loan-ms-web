# Must match API host+port AND the hostname you open in the browser.
# Use localhost everywhere (not 127.0.0.1) or Sanctum CSRF cookies will mismatch.
VITE_API_URL=http://localhost:8000
#
# If API is: php artisan serve --host=localhost --port=8001
# VITE_API_URL=http://localhost:8001
